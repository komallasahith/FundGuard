version: '3.8'

services:
  fundguard-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fundguard-ai
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped
